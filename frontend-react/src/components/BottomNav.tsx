import{Home,Sparkles,Search,Heart,UserRound}from'lucide-react';import type{Page}from'../types';type Props={page:Page;onChange:(page:Page)=>void};const items:[[Page,string,typeof Home],...Array<[Page,string,typeof Home]>]=[['home','خانه',Home],['explore','کشف',Sparkles],['search','جستجو',Search],['saved','علاقه‌مندی‌ها',Heart],['profile','پروفایل',UserRound]];export default function BottomNav({page,onChange}:Props){return <nav className='bottomNav'>{items.map(([id,label,Icon])=><button key={id} className={page===id?'active':''} onClick={()=>onChange(id)}><Icon/><span>{label}</span></button>)}</nav>}