import{openTelegramChannel}from'../data/live';import type{Channel}from'../types';type Props={channels:Channel[]};export default function ChannelRail({channels}:Props){return <section className='surface channelSection'><div className='sectionTitle'><h2>کانال‌های پیشنهادی</h2></div><div className='channelRail'>{channels.map(channel=><article className='channelCard' key={channel.id}><div className={'channelAvatar '+channel.accent}><span>{channel.initials}</span>{channel.avatarUrl&&<img src={channel.avatarUrl} alt='' loading='lazy' onError={event=>event.currentTarget.remove()}/>}</div><strong>{channel.title}</strong><small>{channel.username}</small><button className='followBtn' onClick={()=>openTelegramChannel(channel.username)} disabled={!channel.username}>باز کردن</button></article>)}</div></section>}